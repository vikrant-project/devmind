# Roadmap
- Multi-agent parallelism (planner + coder + reviewer concurrent).
- Self-improvement loop (DevMind analyzes its own reports and tunes prompts).
- Fine-tuned local model trained on DevMind's successful builds.
- Optional GUI alongside CLI.
- Distributed inference across multiple local machines.
- Community tool/template marketplace.
